---
'@teasel/parser': patch
---

a node is 32 bytes instead of 40 and an optional node id four: node ids keep a niche, number values sit beside the tree, and an export of a declaration is its own kind; the writer owns its constant and shape numbering instead of a thread-local, and parenthesized nodes are a bitset
