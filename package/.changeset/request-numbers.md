---
'@teasel/parser': patch
---

the request crosses as numbers: options as one flag word at creation, and a `stopAt` list registered with the source the first time it is seen, its id per parse after; no strings cross per parse
