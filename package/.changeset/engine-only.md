---
'@teasel/parser': patch
---

every parse goes to the engine: the JavaScript-side bare identifier shortcut and the `isIdentifierStart` / `isIdentifierChar` exports are gone, the engine's own tables are the only ones
