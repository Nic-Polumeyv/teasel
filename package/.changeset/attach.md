---
'@teasel/parser': patch
---

a host's own nodes carry no comments, those stay in the list; a comment between a JavaScript node and the host's next syntax trails the node only over blanks, commas and closing parens, so what follows a parenthesis leads the next node
