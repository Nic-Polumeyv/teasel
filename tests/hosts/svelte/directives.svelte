<input bind:value bind:checked={c} class:on class:off={!on} style:color style:width={w} style:height|important="1px" on:click|once|preventDefault={f} use:act={o} transition:t|local in:i out:o animate:flip />
<Comp let:item let:index={i} let:pair={{ a, b }} {...rest} {@attach attach(x)} />
