import type { A } from 'a';
import { type B, C } from 'c';
export type { D };
interface E {}
type F = string;
let g: number = <number>(h as any)!;
function i<T>(this: T, j?: string): void {}
class K<T> implements L { private m: number = 1; declare n: string; constructor(public o: number) {} }
enum P { Q }
namespace R { export const s = 1; }
export = T;
const u = v satisfies W;
abstract class X { abstract y(): void; }
@dec class Z {}
