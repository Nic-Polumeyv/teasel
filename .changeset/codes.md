---
"@teasel/parser": patch
---

`ParseError.code` is the type `Code`, every code the engine reports, so a comparison against one is checked; a grammar that cannot be read throws an `Error` naming the line on both engines.
