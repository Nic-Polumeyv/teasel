---
"@teasel/parser": patch
---

The package is written in TypeScript: `node.d.ts`, `wasm.d.ts` and the declarations under `lib/` are emitted from the sources, in place of a hand-written `types.d.ts`. Entry points are unchanged.
