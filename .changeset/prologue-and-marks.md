---
"@teasel/parser": patch
---

A `"use strict"` string continued on the next line by `|`, `in` or `instanceof` is an expression, not a directive, and one followed by `++` or `--` on the next line is a directive. A literal with many lone surrogate escapes is read in linear time.
